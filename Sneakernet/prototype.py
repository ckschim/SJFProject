#write the log
    #create identity
    #input loop (interruptable)
        #create event
            #seq conter +1
            #timestamp
            #content
            #feed (public_key)
            #previous (hash der vorherigen nachricht)
            #signature
        #save to pcap
    #finisch

#read the log